<script>
  let error = "<%= typeof error != 'undefined' ? error : null %>";
  if (error) {
    alert(error);
  }
  let success = "<%= typeof success != 'undefined' ? success : null %>";
  if (success) {
    alert(success);
  }
</script>
